spring:
  datasource:
    url: jdbc:mysql://gateway01.us-east-1.prod.aws.tidbcloud.com:4000/wissfind?useSSL=true&requireSSL=true&serverTimezone=Asia/Kolkata
    username: oUxEUUYLQY8gA2A.root
    password: N47JhKnuvs9q7k9d
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 30MB

server:
  port: ${SERVER_PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:change-this-development-secret-key-at-least-32-bytes-long}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
  otp:
    ttl-minutes: ${OTP_TTL_MINUTES:10}
    expose-dev: ${OTP_EXPOSE_DEV:true}

management:
  endpoints:
    web:
      exposure:
        include: health,info


payment:
  razorpay:
    key-id: ${RAZORPAY_KEY_ID:}
    key-secret: ${RAZORPAY_KEY_SECRET:}
    webhook-secret: ${RAZORPAY_WEBHOOK_SECRET:}
