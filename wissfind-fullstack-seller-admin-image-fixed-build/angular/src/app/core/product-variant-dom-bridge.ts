type VariantSize = { size: string; sku: string; price: number; oldPrice: number; stock: number };
type VariantColor = { color: string; images: string[]; sizes: VariantSize[] };
export class ProductVariantDomBridge {
 private static installed=false; private static observer?:MutationObserver; private static timer?:number; private static mountedProductId=''; private static readonly cachePrefix='wissfind-variant-cache:'; private static readonly cacheTtl=5*60*1000;
 static install(){if(this.installed||typeof window==='undefined'||typeof document==='undefined')return;this.installed=true;this.updateDetailPendingClass();document.addEventListener('click',e=>this.handleRouteClick(e),true);document.addEventListener('click',e=>this.handleVariantAddClick(e),true);const start=()=>{this.mountIfProductDetail();this.observer=new MutationObserver(()=>{window.clearTimeout(this.timer);this.timer=window.setTimeout(()=>this.mountIfProductDetail(),100)});this.observer.observe(document.body,{childList:true,subtree:true});};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();}
 private static handleRouteClick(event:Event){const target=event.target as HTMLElement|null;const link=target?.closest('a[href]') as HTMLAnchorElement|null;if(!link)return;try{const url=new URL(link.href,window.location.href);if(url.pathname.match(/^\/product\/[^/?#]+/)){document.documentElement.classList.add('variant-detail-pending');}else if(url.pathname!=='/product'&&!url.pathname.startsWith('/product/')){document.documentElement.classList.remove('variant-detail-pending');}}catch{}}
 private static updateDetailPendingClass(){if(location.pathname.match(/^\/product\/[^/?#]+/))document.documentElement.classList.add('variant-detail-pending');else document.documentElement.classList.remove('variant-detail-pending');}
 private static handleVariantAddClick(event:Event){const target=event.target as HTMLElement|null;const button=target?.closest('.btn.add') as HTMLButtonElement|null;if(!button||!button.dataset['variantSku'])return;const stock=Number(button.dataset['variantStock']||0);if(stock<=0){event.preventDefault();event.stopImmediatePropagation();return;}const productId=button.dataset['variantProductId'];if(!productId)return;event.preventDefault();event.stopImmediatePropagation();try{const raw=localStorage.getItem('wissfind-cart');const parsed=JSON.parse(raw||'[]');const items=Array.isArray(parsed)?parsed:[];const variant={color:button.dataset['variantColor']||'',size:button.dataset['variantSize']||'',sku:button.dataset['variantSku']||'',price:Number(button.dataset['variantPrice']||0),oldPrice:Number(button.dataset['variantOldPrice']||0),image:button.dataset['variantImage']||'',stock};const key=`${productId}::${variant.sku||`${variant.color}|${variant.size}`}`;const existing=items.find((x:any)=>`${x?.product?.id}::${x?.variant?.sku||`${x?.variant?.color||''}|${x?.variant?.size||''}`}`===key);if(existing)existing.quantity=Math.max(1,Number(existing.quantity||0)+1);else{const product:any={id:productId,name:document.querySelector('main.page h1')?.textContent?.trim()||'Product',category:'Fashion',price:variant.price,oldPrice:variant.oldPrice,image:variant.image||'',stock:variant.stock,variant};items.push({product,quantity:1,variant});}localStorage.setItem('wissfind-cart',JSON.stringify(items));window.location.href='/cart';}catch{}}
 private static mountIfProductDetail(){const match=location.pathname.match(/^\/product\/([^/?#]+)/);if(!match){this.mountedProductId='';document.documentElement.classList.remove('variant-detail-pending');return;}const productId=decodeURIComponent(match[1]);const main=document.querySelector('main.page') as HTMLElement|null;const copy=main?.querySelector('.copy') as HTMLElement|null;if(!main||!copy)return;if(copy.dataset['variantViewMounted']===productId)return;document.documentElement.classList.add('variant-detail-pending');const gallery=main.querySelector('.gallery') as HTMLElement|null;copy.querySelector('.product-variant-view')?.remove();copy.querySelectorAll('.option').forEach(x=>(x as HTMLElement).style.display='none');const section=document.createElement('section');section.className='product-variant-view';section.dataset['productId']=productId;section.innerHTML=this.template();const addButton=copy.querySelector('.btn.add');if(addButton)copy.insertBefore(section,addButton);else copy.appendChild(section);copy.dataset['variantViewMounted']=productId;this.mountedProductId=productId;void this.load(productId,section,main);}
 private static template(){return `<style>.product-variant-view{.product-variant-view{margin:22px 0 16px;padding:16px 0;border-top:1px solid var(--line,#e5e5e5);border-bottom:1px solid var(--line,#e5e5e5)}.pvv-block{margin-bottom:17px}.pvv-block:last-child{margin-bottom:0}.pvv-title{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:9px;font-size:14px}.pvv-selected{font-size:12px;color:#777;font-weight:500}.pvv-swatches{display:flex;flex-wrap:wrap;gap:9px}.pvv-swatch{border:1px solid #d9d9d9;background:#fff;color:#111;border-radius:9px;padding:9px 13px;cursor:pointer;font:inherit;font-size:13px;transition:.15s}.pvv-swatch:hover{border-color:#111}.pvv-swatch.active{border-color:#111;box-shadow:0 0 0 1px #111;font-weight:800;background:#f7f7f5}.pvv-swatch:focus-visible{outline:2px solid #111;outline-offset:2px}.pvv-swatch.out{opacity:.55}.pvv-info{display:none}.pvv-status{display:none}.pvv-stock-note{font-size:11px;color:#777;margin-top:9px}</style><div class="pvv-block pvv-color-block"><div class="pvv-title"><strong>Color</strong><span class="pvv-selected pvv-color-selected">Select color</span></div><div class="pvv-swatches pvv-colors"></div></div><div class="pvv-block pvv-size-block"><div class="pvv-title"><strong>Size</strong><span class="pvv-selected pvv-size-selected">Select size</span></div><div class="pvv-swatches pvv-sizes"></div><div class="pvv-info"><span>SKU: <b class="pvv-sku">—</b></span><span>Stock: <b class="pvv-stock">—</b></span></div><div class="pvv-stock-note">Select a color and size to see variant price, MRP, SKU and stock.</div></div><div class="pvv-status"></div>`;}
 private static async load(productId:string,section:HTMLElement,main:HTMLElement){const gallery=main.querySelector('.gallery') as HTMLElement|null;const parentImage=main.querySelector('.main-image img') as HTMLImageElement|null;const parentSrc=parentImage?.currentSrc||parentImage?.src||'';try{const data=await this.getCachedVariants(productId);const raw=Array.isArray(data)?data:(Array.isArray(data?.content)?data.content:(Array.isArray(data?.variants)?data.variants:[]));const variants:VariantColor[]=raw.map((v:any)=>({color:String(v?.color??v?.name??'').trim(),images:Array.isArray(v?.images)?v.images.map((x:any)=>this.absoluteUrl(String(x??''))).filter(Boolean):[],sizes:Array.isArray(v?.sizes)?v.sizes.map((s:any)=>({size:String(s?.size??s?.name??'').trim(),sku:String(s?.sku??'').trim(),price:Number(s?.price??0),oldPrice:Number(s?.oldPrice??s?.mrp??0),stock:Math.max(0,Number(s?.stock??0))})).filter((s:VariantSize)=>s.size):[]})).filter((v:VariantColor)=>v.color&&v.sizes.length);if(!variants.length){section.remove();main.querySelectorAll('.copy .option').forEach(x=>(x as HTMLElement).style.display='');if(gallery){gallery.classList.remove('variant-pending-hide');gallery.style.visibility='';}document.documentElement.classList.remove('variant-detail-pending');return;}const first=variants.find(v=>v.sizes.some(s=>s.stock>0&&v.images.length>0))||variants.find(v=>v.images.length>0)||variants[0];const firstAvailable=first.sizes.find(s=>s.stock>0)||first.sizes[0];const firstImage=first.images[0]||'';this.render(section,main,variants,first,firstAvailable);if(gallery){gallery.classList.remove('variant-pending-hide');gallery.style.visibility='';}document.documentElement.classList.remove('variant-detail-pending');}catch{section.remove();if(parentSrc&&parentImage){parentImage.src=parentSrc;this.restoreParentGallery(main);}else if(gallery){gallery.classList.remove('variant-pending-hide');gallery.style.visibility='';}document.documentElement.classList.remove('variant-detail-pending');}}
 private static preloadImage(url:string):Promise<boolean>{return new Promise(resolve=>{const img=new Image();let done=false;const finish=(ok:boolean)=>{if(done)return;done=true;resolve(ok);};img.onload=()=>finish(true);img.onerror=()=>finish(false);img.src=url;});}
 private static restoreParentGallery(main:HTMLElement){const gallery=main.querySelector('.gallery') as HTMLElement|null;if(!gallery)return;gallery.classList.remove('variant-pending-hide');gallery.style.visibility='';}
 private static render(section:HTMLElement,main:HTMLElement,variants:VariantColor[],selectedColor:VariantColor,selectedSize:VariantSize){const colors=section.querySelector('.pvv-colors') as HTMLElement;const sizes=section.querySelector('.pvv-sizes') as HTMLElement;const colorText=section.querySelector('.pvv-color-selected') as HTMLElement;const sizeText=section.querySelector('.pvv-size-selected') as HTMLElement;const sku=section.querySelector('.pvv-sku') as HTMLElement;const stock=section.querySelector('.pvv-stock') as HTMLElement;const status=section.querySelector('.pvv-status') as HTMLElement;colors.innerHTML='';variants.forEach(v=>{const b=document.createElement('button');b.type='button';b.className='pvv-swatch';b.textContent=v.color;b.setAttribute('aria-pressed',String(v.color.toLowerCase()===selectedColor.color.toLowerCase()));if(v.color.toLowerCase()===selectedColor.color.toLowerCase())b.classList.add('active');b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();this.render(section,main,variants,v,v.sizes.find(s=>s.stock>0)||v.sizes[0])});colors.appendChild(b)});sizes.innerHTML='';const safe=selectedSize&&selectedColor.sizes.some(s=>s.sku===selectedSize.sku)?selectedSize:(selectedColor.sizes.find(s=>s.stock>0)||selectedColor.sizes[0]);selectedColor.sizes.forEach(s=>{const b=document.createElement('button');b.type='button';b.className='pvv-swatch';b.textContent=s.size;b.setAttribute('aria-label',`Select size ${s.size}`);b.setAttribute('aria-pressed',String(!!safe&&s.sku===safe.sku));if(safe&&s.sku===safe.sku)b.classList.add('active');if(s.stock<=0){b.classList.add('out');b.title='Out of stock';}b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();this.render(section,main,variants,selectedColor,s)});sizes.appendChild(b)});colorText.textContent=`Selected: ${selectedColor.color}`;sizeText.textContent=safe?.size?`Selected: ${safe.size}`:'Select size';sku.textContent=safe?.sku||'—';stock.textContent=safe?String(safe.stock):'—';this.updatePrice(main,safe);const addButton=main.querySelector('.btn.add') as HTMLButtonElement|null;if(addButton&&safe){addButton.dataset['variantProductId']=section.dataset['productId']||'';addButton.dataset['variantColor']=selectedColor.color;addButton.dataset['variantSize']=safe.size;addButton.dataset['variantSku']=safe.sku;addButton.dataset['variantPrice']=String(safe.price);addButton.dataset['variantOldPrice']=String(safe.oldPrice);addButton.dataset['variantStock']=String(safe.stock);addButton.dataset['variantImage']=selectedColor.images[0]||'';addButton.disabled=safe.stock<=0;addButton.textContent=safe.stock>0?'Add to bag':'Out of stock';}status.textContent=safe&&safe.stock<=0?'This size is currently out of stock.':'';status.classList.toggle('error',!!safe&&safe.stock<=0);this.setGallery(main,selectedColor.images);this.pauseAuto(main);}
 private static updatePrice(main:HTMLElement,size?:VariantSize){const priceRow=main.querySelector('.price') as HTMLElement|null;if(!priceRow||!size)return;let price=priceRow.querySelector('strong') as HTMLElement|null;let oldPrice=priceRow.querySelector('del') as HTMLElement|null;if(!price){price=document.createElement('strong');priceRow.prepend(price)}if(!oldPrice){oldPrice=document.createElement('del');priceRow.appendChild(oldPrice)}price.textContent=`₹${size.price.toLocaleString('en-IN')}`;if(size.oldPrice>size.price){oldPrice.textContent=`₹${size.oldPrice.toLocaleString('en-IN')}`;oldPrice.style.display=''}else{oldPrice.textContent='';oldPrice.style.display='none';}}
 private static setGallery(main:HTMLElement,images:string[]){if(!images.length)return;const thumbs=main.querySelector('.thumbs') as HTMLElement|null;const mainImage=main.querySelector('.main-image img') as HTMLImageElement|null;const progress=main.querySelector('.slide-progress') as HTMLElement|null;const count=main.querySelector('.image-count') as HTMLElement|null;if(!thumbs||!mainImage)return;thumbs.innerHTML='';thumbs.style.cssText=`display:${images.length>1?'flex':'none'};gap:8px;width:100%;overflow-x:auto;padding:3px 2px 5px;box-sizing:border-box;scrollbar-width:none;`;images.forEach((image,index)=>{const b=document.createElement('button');b.type='button';b.className=`thumb${index===0?' active':''}`;b.title=`View image ${index+1}`;b.style.cssText='flex:0 0 64px;width:64px;height:64px;padding:0;border:1px solid #ddd;border-radius:8px;background:#fff;overflow:hidden;cursor:pointer;box-sizing:border-box;opacity:.82;';const img=document.createElement('img');img.src=image;img.alt=`Product variant image ${index+1}`;img.style.cssText='width:100%;height:100%;display:block;object-fit:contain;background:#fff;';b.appendChild(img);b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();this.selectGalleryImage(main,images,index)});thumbs.appendChild(b)});if(progress)progress.innerHTML=images.map((_,i)=>`<span class="${i===0?'active':''}"></span>`).join('');this.selectGalleryImage(main,images,0);if(count)count.textContent=`1 / ${images.length}`;}
 private static selectGalleryImage(main:HTMLElement,images:string[],index:number){const img=main.querySelector('.main-image img') as HTMLImageElement|null;const thumbs=main.querySelectorAll('.thumb');const progress=main.querySelectorAll('.slide-progress span');if(!img||!images.length)return;const safe=Math.max(0,Math.min(index,images.length-1));img.src=images[safe];thumbs.forEach((x,i)=>x.classList.toggle('active',i===safe));progress.forEach((x,i)=>x.classList.toggle('active',i===safe));const count=main.querySelector('.image-count') as HTMLElement|null;if(count)count.textContent=`${safe+1} / ${images.length}`;}
 private static pauseAuto(main:HTMLElement){const b=main.querySelector('.auto-btn') as HTMLButtonElement|null;if(b&&b.textContent?.includes('Pause'))b.click();}
 private static async getCachedVariants(productId:string){
  const key=this.cachePrefix+productId;
  try{const raw=localStorage.getItem(key);if(raw){const cached=JSON.parse(raw);if(Date.now()-Number(cached?.at||0)<=this.cacheTtl)return cached.data;}}catch{}
  const data=await this.api(`/products/${encodeURIComponent(productId)}/variants`);
  try{localStorage.setItem(key,JSON.stringify({at:Date.now(),data}));}catch{}
  return data;
 } private static async api(path:string){const token=localStorage.getItem('wissfind_jwt');const headers:Record<string,string>=token?{Authorization:`Bearer ${token}`}:{ };const r=await fetch(`${this.baseUrl()}${path}`,{headers});if(!r.ok)throw new Error(`Request failed (${r.status})`);return r.json();}
 private static baseUrl(){return typeof window!=='undefined'&&window.location.hostname==='localhost'&&window.location.port==='4200'?'http://localhost:8080/api':'/api';}
 private static absoluteUrl(url:string){if(!url)return'';if(/^https?:\/\//i.test(url))return url;if(url.startsWith('/'))return`${this.baseUrl().replace(/\/api$/,'')}${url}`;return`${this.baseUrl().replace(/\/api$/,'')}/${url}`;}
}
